---
title: Metrics Admin
slug: /metrics-admin
---

## Metrics Admin

import MetricsAdminUI from '@site/src/components/MetricsAdminUI';

This page displays the local admin UI for editing metrics. It is intended for local development only.

<MetricsAdminUI />

> Note: During local development Docusaurus is configured to proxy `/api` requests to the metrics server (see `docusaurus.config.ts`).
