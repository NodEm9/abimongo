{
	"name": "@abimongo/cli",
	"version": "1.0.0",
	"publishConfig": {
	"registry": "http://localhost:4873/",
	"access": "public"
		},
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"bin": {
		"abimongo-cli": "./bin/abimongo_cli.js"
	},
	"files": [
		"dist",
		"bin",
		"README.md",
		"LICENSE"
	],
	"type": "commonjs",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"lint": "eslint src",
		"prepack": "pnpm build",
		"publish": " pnpm --recursive publish --no-git-checks"
	},
	"dependencies": {
		"@abimongo/core": "workspace:^1.0.0",
		"@abimongo/create": "workspace:^1.0.0",
		"@abimongo/logger": "workspace:^1.0.0",
		"commander": "^14.0.2"
	},
	"engines": {
		"node": ">=20.11.0"
	},
	"license": "ISC",
	"description": "CLI tooling for Abimongo (shim + scaffold command)."
}