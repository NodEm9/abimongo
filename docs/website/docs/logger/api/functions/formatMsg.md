[**@abimongo/logger**](../README.md)

***

# Function: formatMsg()

> **formatMsg**(`level`, `message`, `meta`, `options?`): `string`

Defined in: [utils/formatters.ts:12](https://github.com/NodEm9/abimongo/blob/e9f185bc0c40bc037acc1c4cf3d4db09dd62229c/packages/logger/src/utils/formatters.ts#L12)

Formats a log message based on the provided options.

## Parameters

### level

[`LogLevel`](../type-aliases/LogLevel.md)

The log level of the message.

### message

`string`

The log message.

### meta

`any`[]

Additional metadata for the log message.

### options?

[`LoggerFormatOptions`](../interfaces/LoggerFormatOptions.md)

Formatting options.

## Returns

`string`

The formatted log message as a string.
