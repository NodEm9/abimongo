## Archived package workflow template: packages/core/.github/workflows/release.yml
## Moved to .github/ci-templates/ as a template; not executed by Actions here.

name: Release

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
    contents: write

jobs:
  release:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup 
        uses: actions/setup-node@v4
      - uses: actions/setup-node@v4
        with:
         node-version: 20
         cache: 'pnpm'

      - run: corepack enable
      - run: corepack install

      - name: Install (workspaces)
        run: pnpm install --frozen-lockfile --prefer-workspace-packages
      - name: Create Release Pull Request
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for semantic release
      
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Run Semantic Release
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          2FACTOR_TOKEN: ${{ secrets.NPM_2FACTOR_TOKEN }} 
          NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
        run: npx semantic-release
