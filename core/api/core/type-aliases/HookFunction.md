# Type Alias: HookFunction()

> **HookFunction** = (`data`) => `Promise`\<`void`\>

Defined in: [core/src/types/schema.type.ts:24](https://github.com/NodEm9/abimongo/blob/3e138ae382a8e575025463fa0581dc3e88325be9/packages/core/src/types/schema.type.ts#L24)

Represents a hook function for middleware.

## Parameters

### data

`any`

The data passed to the hook.

## Returns

`Promise`\<`void`\>

A promise that resolves when the hook is executed.
