# Type Alias: RemoteTransporter()

> **RemoteTransporter** = (`formattedMessage`, `meta`) => `Promise`\<`void`\>

Defined in: types/logger.types.ts:22

## Parameters

### formattedMessage

`string`

### meta

[`LogEntry`](../interfaces/LogEntry.md)

## Returns

`Promise`\<`void`\>
